{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug Run",
      "program": "${workspaceFolder:next-chatbot-server}/.build/debug/Run",
      "args": [],
      "cwd": "${workspaceFolder:next-chatbot-server}",
      "preLaunchTask": "swift: Build Debug Run"
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "Release Run",
      "program": "${workspaceFolder:next-chatbot-server}/.build/release/Run",
      "args": [],
      "cwd": "${workspaceFolder:next-chatbot-server}",
      "preLaunchTask": "swift: Build Release Run"
    }
  ]
}
